
import { User, ParkingSpace, Booking } from '../types';

// All fake data removed as per request. 
// System now relies on real-time DB persistence.
export const mockUsers: User[] = [];
export const mockSpaces: ParkingSpace[] = [];
export const mockBookings: Booking[] = [];
